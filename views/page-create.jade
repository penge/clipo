extends layout

block content
  .container
    h1.unselectable
      span #{name}
      span.message  available!

    form
      input#password-fake.hide(type="password")
      input#password(type="password", placeholder="Password")

    a.large#create-page-button(href="#", data-page-name=name) Get #{name}